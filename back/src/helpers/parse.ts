/**
 * @summary Parse text string and convert it to json finding ytInitialData
 * @summary Filter by '"playlist":{"title": "..."}'
 * @param text
 */
export const parseYTSourceText = (text: string) => {
  console.log('test', text);
};
